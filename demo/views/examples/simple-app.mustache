<!-- Page content only — the layout (with simpleLayout:true) supplies the
     sidebar-less shell, the header (no burger / no menu), the settings panel,
     profile panel, command palette, and all demo JS. So every real setting —
     Theme, Layout Width, Search Box, RTL, Profile Panel, Font size/family —
     works here exactly as in the full demo. -->

<div class="pa-callout pa-callout--info mb-4">
    <div class="pa-callout__body">
        <strong>Minimal shell — no sidebar, no burger.</strong> The header brand sits
        flush-left and this content spans the full width (no sidebar column).
        Open <strong>⚙ Settings</strong> (top-right) and try <em>Layout Width</em>,
        <em>Search Box</em>, <em>Display → RTL</em>, <em>Profile Panel</em>, and
        <em>Font size / family</em> — they all drive this page live. Narrow the
        window to watch the fit engine drop the version tag, then the page title.
    </div>
</div>

<div class="pc-row mb-4">
    <div class="pc-col-1-3">
        <div class="pa-card"><div class="pa-card__body">
            <div class="pa-stat"><div class="pa-stat__value">1,284</div><div class="pa-stat__label">Users</div></div>
        </div></div>
    </div>
    <div class="pc-col-1-3">
        <div class="pa-card"><div class="pa-card__body">
            <div class="pa-stat"><div class="pa-stat__value">$42.9k</div><div class="pa-stat__label">Revenue</div></div>
        </div></div>
    </div>
    <div class="pc-col-1-3">
        <div class="pa-card"><div class="pa-card__body">
            <div class="pa-stat"><div class="pa-stat__value">3.1%</div><div class="pa-stat__label">Churn</div></div>
        </div></div>
    </div>
</div>

<div class="pa-card">
    <div class="pa-card__header"><div class="pa-card__title"><span class="pa-card__title-text">Recent activity</span></div></div>
    <div class="pa-card__body">
        <p>Full-width content — there's no sidebar taking a column, so the card
        stretches edge to edge. This is the whole "simple app" recipe: keep the
        header + a full-width <code>.pa-layout__content</code>, drop the aside.
        Constrain it with the <em>Layout Width</em> setting if you want a centered,
        capped column instead.</p>
    </div>
</div>
